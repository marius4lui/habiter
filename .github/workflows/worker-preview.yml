name: Release API Preview

on:
  pull_request:
    types: [opened, synchronize, reopened]
    paths:
      - 'apps/release-api/**'
      - 'packages/release-core/**'
      - 'scripts/release/**'
      - 'package.json'
      - 'pnpm-lock.yaml'
      - 'pnpm-workspace.yaml'
      - '.github/workflows/worker-preview.yml'

permissions:
  contents: read
  pull-requests: write

concurrency:
  group: release-api-preview-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  preview:
    name: Deploy Native Preview Version
    if: github.event.pull_request.head.repo.full_name == github.repository
    runs-on: ubuntu-latest
    timeout-minutes: 20
    outputs:
      url: ${{ steps.deploy.outputs.url }}
    steps:
      - uses: actions/checkout@v7
      - uses: pnpm/action-setup@v6
        with:
          version: 11.21.0
      - uses: actions/setup-node@v7
        with:
          node-version: '24.13.1'
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm release:validate
      - run: node scripts/release/sync-worker-data.mjs --preview
      - run: pnpm --filter @habiter/release-api types
      - run: pnpm --filter @habiter/release-api check
      - name: Deploy preview
        id: deploy
        working-directory: apps/release-api
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          PREVIEW_ALIAS: pr-${{ github.event.pull_request.number }}
        run: |
          pnpm exec wrangler versions upload \
            --env preview \
            --preview-alias "$PREVIEW_ALIAS" \
            --tag "$PREVIEW_ALIAS" \
            --message "Pull request #${{ github.event.pull_request.number }} at ${GITHUB_SHA}" \
            | tee deploy.log
          PREVIEW_URL=$(grep -Eo 'https://[^ ]+\.workers\.dev' deploy.log | tail -1)
          test -n "$PREVIEW_URL"
          echo "url=$PREVIEW_URL" >> "$GITHUB_OUTPUT"
          curl --fail --retry 5 --retry-all-errors --retry-delay 3 "$PREVIEW_URL/health"
          curl --fail --retry 5 --retry-all-errors --retry-delay 3 "$PREVIEW_URL/api/v1/releases/latest"
          curl --fail --retry 5 --retry-all-errors --retry-delay 3 "$PREVIEW_URL/api/v1/manifest"
      - name: Create or update preview comment
        uses: actions/github-script@v9
        env:
          PREVIEW_URL: ${{ steps.deploy.outputs.url }}
        with:
          script: |
            const marker = '<!-- habiter-release-api-preview -->';
            const body = `${marker}\n✅ Native Cloudflare Release API preview: ${process.env.PREVIEW_URL}`;
            const comments = await github.paginate(github.rest.issues.listComments, {
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
            });
            const existing = comments.find((comment) => comment.body?.includes(marker));
            if (existing) {
              await github.rest.issues.updateComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                comment_id: existing.id,
                body,
              });
            } else {
              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: context.issue.number,
                body,
              });
            }
