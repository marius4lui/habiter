{
  "name": "@habiter/release-api",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "pnpm types:check && tsc --noEmit && vitest run",
    "test": "vitest run",
    "types": "wrangler types worker-configuration.d.ts",
    "types:check": "wrangler types worker-configuration.d.ts --check",
    "deploy:dry": "wrangler deploy --env production --dry-run"
  },
  "devDependencies": {
    "typescript": "^7.0.2",
    "vitest": "^4.1.11",
    "wrangler": "4.126.0"
  }
}
