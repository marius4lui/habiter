{
  "name": "@habiter/sync-d1",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "exports": "./src/index.ts",
  "scripts": {
    "build:test-worker": "esbuild test/worker.ts --bundle --format=esm --platform=browser --target=es2022 --outfile=.tmp/worker.mjs",
    "migrations:check": "node scripts/check-local-migrations.mjs",
    "check": "tsc --noEmit && pnpm migrations:check && pnpm build:test-worker && vitest run",
    "test": "pnpm build:test-worker && vitest run"
  },
  "dependencies": {
    "@habiter/sync-core": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^26.3.0",
    "esbuild": "^0.28.2",
    "miniflare": "3.20250718.3",
    "typescript": "^7.0.2",
    "vitest": "^4.1.11",
    "wrangler": "4.125.0"
  }
}
